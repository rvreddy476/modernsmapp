openapi: 3.0.3
info:
  title: Mopedu Mobility Platform API
  version: 1.0.0
  description: Authoritative REST API for Mopedu Rider & Captain Mobility Services. All monetary amounts are in integer paise (INR).
servers:
  - url: https://api.atpost.app
    description: Production API Gateway
  - url: http://localhost:8080
    description: Local API Gateway

paths:
  /v1/rider/cities:
    get:
      summary: List serviceable cities
      operationId: listCities
      tags: [Public]
      responses:
        '200':
          description: List of serviceable cities
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/City'

  /v1/rider/serviceability:
    get:
      summary: Check coordinates serviceability
      operationId: checkServiceability
      tags: [Public]
      parameters:
        - name: lat
          in: query
          required: true
          schema:
            type: number
            format: double
        - name: lng
          in: query
          required: true
          schema:
            type: number
            format: double
      responses:
        '200':
          description: Serviceability response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServiceabilityResponse'

  /v1/rider/estimate:
    post:
      summary: Request authoritative fare quote
      operationId: estimateFare
      tags: [Rider]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EstimateRequest'
      responses:
        '200':
          description: >-
            Quote snapshot with vehicle options. Every amount is integer paise;
            each option carries the itemised breakdown that is locked for the
            ride (fare window / surge, coupon discount, previous cancellation
            fees, tax lines). Golden bytes: rider-service
            internal/http/testdata/contracts/estimate_peak_window_coupon.json.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuoteSnapshotResponse'
        '422':
          description: The coupon_code was refused (see CouponError codes)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CouponError'

  /v1/rider/coupons/validate:
    get:
      summary: Validate a coupon code before quoting
      operationId: validateCoupon
      tags: [Public]
      description: >-
        Public; with a gateway identity the first-ride-only and per-user
        limits are checked too. The fare-dependent minimum-fare rule is only
        applied by the estimate. Golden bytes for the 422:
        rider-service internal/http/testdata/contracts/coupon_validate_error.json.
      parameters:
        - name: code
          in: query
          required: true
          schema:
            type: string
        - name: city_id
          in: query
          required: false
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: The coupon is valid for this caller
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CouponValidation'
        '422':
          description: The coupon was refused
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CouponError'

  /v1/rider/rides:
    post:
      summary: Create or book a ride
      operationId: createRide
      tags: [Rider]
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateRideRequest'
      responses:
        '201':
          description: >-
            Ride booked successfully. The quote's coupon redemption is
            reserved and its outstanding fees are bound to the ride in the
            same transaction; the locked breakdown is copied to
            Ride.fare_breakdown.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Ride'
        '400':
          description: >-
            payment_method not in cash, upi, card (wallet is not a ride
            method), or the quote is missing / expired / for another user
        '422':
          description: The quote's coupon can no longer be reserved (limits raced)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CouponError'

  /v1/rider/rides/active:
    get:
      summary: Get caller's active ride snapshot
      operationId: getActiveRide
      tags: [Rider]
      responses:
        '200':
          description: Active ride details or active=false
          content:
            application/json:
              schema:
                type: object
                properties:
                  active:
                    type: boolean
                  ride:
                    $ref: '#/components/schemas/Ride'

  /v1/rider/rides/me:
    get:
      summary: List recent rides
      operationId: listMyRides
      tags: [Rider]
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
      responses:
        '200':
          description: List of rides
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Ride'

  /v1/rider/rides/{id}:
    get:
      summary: Get ride by ID
      operationId: getRide
      tags: [Rider]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Ride details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Ride'

  /v1/rider/rides/{id}/receipt:
    get:
      summary: Get immutable trip receipt
      operationId: getRideReceipt
      tags: [Rider]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: >-
            Trip receipt in integer paise. distance_meters / duration_seconds
            are what was priced (the quoted route, or the server-tracked
            route when completion re-priced it); the captain's report is
            telemetry (reported_*). Golden bytes: rider-service
            internal/http/testdata/contracts/receipt_waiting_charge.json.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RideReceipt'

  /v1/rider/rides/{id}/cancel:
    post:
      summary: Cancel a ride
      operationId: cancelRide
      tags: [Rider]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                idempotency_key:
                  type: string
      responses:
        '200':
          description: Cancellation confirmed

  /v1/rider/rides/{id}/rate:
    post:
      summary: Rate a completed trip
      operationId: rateRide
      tags: [Rider]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [rating]
              properties:
                rating:
                  type: integer
                  minimum: 1
                  maximum: 5
                feedback:
                  type: string
      responses:
        '200':
          description: Rating recorded

  /v1/rider/offers/incoming:
    get:
      summary: Get incoming ride offers for online captain
      operationId: getIncomingOffers
      tags: [Captain]
      responses:
        '200':
          description: List of pending offers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/RideOffer'

  /v1/rider/offers/{id}/accept:
    post:
      summary: Accept a dispatched offer
      operationId: acceptOffer
      tags: [Captain]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Offer accepted; ride assigned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AcceptOfferResponse'

  /v1/rider/offers/{id}/reject:
    post:
      summary: Reject an offer
      operationId: rejectOffer
      tags: [Captain]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Offer rejected

  /v1/rider/rides/{id}/arriving:
    post:
      summary: Mark captain is arriving at pickup
      operationId: markArriving
      tags: [Captain]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Status updated to partner_arriving

  /v1/rider/rides/{id}/arrived:
    post:
      summary: Mark captain has arrived at pickup
      operationId: markArrived
      tags: [Captain]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Status updated to arrived

  /v1/rider/rides/{id}/start:
    post:
      summary: Verify OTP and start trip
      operationId: startRide
      tags: [Captain]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [otp]
              properties:
                otp:
                  type: string
                  example: "4829"
      responses:
        '200':
          description: OTP verified; trip started

  /v1/rider/rides/{id}/complete:
    post:
      summary: Complete trip and submit final telemetry
      operationId: completeRide
      tags: [Captain]
      description: >-
        The fare is upfront: the quote's locked total plus the waiting charge
        (minutes beyond the free window between arrival and start) and any
        previous cancellation fee, minus the coupon. Distance and time are
        re-priced ONLY when the server-tracked route exceeds the quoted
        distance by more than 20% and more than 1 km, at the locked surge,
        with the ride fare capped at twice the quoted one.
        final_distance_km / final_duration_min are stored as telemetry and
        never price the ride.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [final_distance_km, final_duration_min, idempotency_key]
              properties:
                final_distance_km:
                  type: number
                final_duration_min:
                  type: integer
                idempotency_key:
                  type: string
      responses:
        '200':
          description: Trip completed; payment generated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RidePayment'

  /v1/rider/rides/{id}/payment/cash-confirm:
    post:
      summary: Captain confirms receipt of cash fare
      operationId: confirmCashPayment
      tags: [Captain]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Cash receipt confirmed

  /v1/rider/partners/me/online:
    post:
      summary: Transition captain to online status
      operationId: goOnline
      tags: [Captain]
      responses:
        '200':
          description: Captain is online

  /v1/rider/partners/me/offline:
    post:
      summary: Transition captain to offline status
      operationId: goOffline
      tags: [Captain]
      responses:
        '200':
          description: Captain is offline

  /v1/rider/partners/me/location:
    post:
      summary: Ingest captain GPS location telemetry
      operationId: updateLocation
      tags: [Captain]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [lat, lng]
              properties:
                lat:
                  type: number
                lng:
                  type: number
                speed_mps:
                  type: number
                heading:
                  type: number
                accuracy_meters:
                  type: number
                sequence:
                  type: integer
      responses:
        '200':
          description: Telemetry ingested

  /v1/rider/rides/{id}/sos:
    post:
      summary: Trigger emergency SOS alert
      operationId: triggerSOS
      tags: [Safety]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                lat:
                  type: number
                lng:
                  type: number
                reason:
                  type: string
      responses:
        '201':
          description: SOS incident logged and dispatched

  /v1/rider/rides/{id}/share:
    post:
      summary: Create live trip sharing link
      operationId: createShareToken
      tags: [Safety]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '201':
          description: Share link created
    delete:
      summary: Revoke active share links for ride
      operationId: revokeShareToken
      tags: [Safety]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Share links revoked

  /v1/rider/share/{token}:
    get:
      summary: Public redacted live trip view
      operationId: getSharedRide
      tags: [Public]
      parameters:
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Redacted live ride view
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SharedRideView'

  # Admin console (admin-service tokens only; the legacy /v1/rider/admin/*
  # family serves the same handlers). Every call is audited. Permissions:
  # coupons -> rider:fares.manage, outstanding list -> rider:payments.read,
  # waive -> rider:payments.settle.
  /v1/rider/internal/admin/coupons:
    get:
      summary: List coupons
      operationId: adminListCoupons
      tags: [Admin]
      parameters:
        - { name: active, in: query, schema: { type: boolean } }
        - { name: limit, in: query, schema: { type: integer } }
        - { name: offset, in: query, schema: { type: integer } }
      responses:
        '200':
          description: '{"items": [Coupon]}'
    post:
      summary: Create a coupon
      operationId: adminCreateCoupon
      tags: [Admin]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CouponCreate'
      responses:
        '201':
          description: Coupon
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Coupon'
  /v1/rider/internal/admin/coupons/redemptions:
    get:
      summary: List coupon redemptions
      operationId: adminListCouponRedemptions
      tags: [Admin]
      parameters:
        - { name: coupon_id, in: query, schema: { type: string, format: uuid } }
      responses:
        '200':
          description: '{"items": [{id, coupon_id, customer_user_id, ride_id, quote_id, discount_paise, status: reserved|applied|released}]}'
  /v1/rider/internal/admin/coupons/{id}:
    patch:
      summary: Update a coupon (partial)
      operationId: adminUpdateCoupon
      tags: [Admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        '200':
          description: Coupon
  /v1/rider/internal/admin/coupons/{id}/deactivate:
    post:
      summary: Deactivate a coupon
      operationId: adminDeactivateCoupon
      tags: [Admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        '200':
          description: Coupon
  /v1/rider/internal/admin/outstanding:
    get:
      summary: List customers' outstanding cancellation fees
      operationId: adminListOutstanding
      tags: [Admin]
      parameters:
        - { name: customer_id, in: query, schema: { type: string, format: uuid } }
        - { name: status, in: query, schema: { type: string, enum: [pending, settled, waived] } }
      responses:
        '200':
          description: '{"items": [CustomerOutstanding]}'
  /v1/rider/internal/admin/outstanding/{id}/waive:
    post:
      summary: Waive a pending cancellation fee
      operationId: adminWaiveOutstanding
      tags: [Admin]
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason:
                  type: string
      responses:
        '200':
          description: CustomerOutstanding with status waived

components:
  schemas:
    City:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        state:
          type: string
        country:
          type: string
        currency_code:
          type: string
        is_active:
          type: boolean

    ServiceabilityResponse:
      type: object
      properties:
        serviceable:
          type: boolean
        city_id:
          type: string
          format: uuid
        city_name:
          type: string
        supported_vehicle_types:
          type: array
          items:
            type: string

    EstimateRequest:
      type: object
      required: [pickup_lat, pickup_lng, drop_lat, drop_lng, city_id]
      properties:
        pickup_lat:
          type: number
        pickup_lng:
          type: number
        pickup_label:
          type: string
        drop_lat:
          type: number
        drop_lng:
          type: number
        drop_label:
          type: string
        vehicle_type:
          type: string
        city_id:
          type: string
          format: uuid
        coupon_code:
          type: string
          description: Case-insensitive. Refused with 422 CouponError.

    TaxLine:
      type: object
      properties:
        ref:
          type: string
          enum: [ride_fare, platform_fee, waiting_charge, previous_cancellation_fee, cancellation_fee]
        category:
          type: string
          description: PASSENGER_TRANSPORT_VIA_ECO (5%, s.9(5), SAC 996412) or PLATFORM_FEE (18%)
        sac:
          type: string
        rate_bps:
          type: integer
          format: int64
        taxable_paise:
          type: integer
          format: int64
        tax_paise:
          type: integer
          format: int64
        gross_paise:
          type: integer
          format: int64

    QuoteBreakdownPaise:
      type: object
      description: >-
        The itemised fare, stored verbatim on the quote and on the ride.
        ride_fare = base + distance + time + surge (+ minimum top-up);
        taxable_ride_fare = ride_fare - discount; total = taxable_ride_fare
        + platform_fee + waiting_charge + outstanding + tax. Tax lines sum to
        tax_paise. tax_note is always "adviser confirmation pending".
      properties:
        base_paise:
          type: integer
          format: int64
        distance_paise:
          type: integer
          format: int64
        time_paise:
          type: integer
          format: int64
        surge_paise:
          type: integer
          format: int64
        minimum_top_up_paise:
          type: integer
          format: int64
        ride_fare_paise:
          type: integer
          format: int64
        discount_paise:
          type: integer
          format: int64
        coupon_code:
          type: string
        coupon_id:
          type: string
        taxable_ride_fare_paise:
          type: integer
          format: int64
        platform_fee_paise:
          type: integer
          format: int64
        waiting_minutes:
          type: integer
        waiting_charge_paise:
          type: integer
          format: int64
        outstanding_paise:
          type: integer
          format: int64
          description: Previous cancellation fees charged on this ride
        outstanding_ids:
          type: array
          items:
            type: string
        toll_paise:
          type: integer
          format: int64
        tax_paise:
          type: integer
          format: int64
        tax_lines:
          type: array
          items:
            $ref: '#/components/schemas/TaxLine'
        tax_note:
          type: string
        surge_basis_points:
          type: integer
          format: int64
          description: Extra over the base fare, 2500 = x1.25. Window and demand never stack.
        surge_reason:
          type: string
          enum: [none, peak_hours, high_demand]
        window_name:
          type: string
        distance_meters:
          type: integer
        duration_seconds:
          type: integer
        total_paise:
          type: integer
          format: int64
        recomputed_from_track:
          type: boolean
        cap_applied:
          type: boolean
        fare_policy_version:
          type: integer

    QuoteOption:
      type: object
      properties:
        vehicle_type:
          type: string
        available:
          type: boolean
        pickup_eta_seconds:
          type: integer
        distance_meters:
          type: integer
        duration_seconds:
          type: integer
        currency:
          type: string
        total_paise:
          type: integer
          format: int64
        surge_bps:
          type: integer
          format: int64
        surge_reason:
          type: string
          enum: [none, peak_hours, high_demand]
        window_name:
          type: string
        discount_paise:
          type: integer
          format: int64
        coupon_code:
          type: string
        breakdown:
          $ref: '#/components/schemas/QuoteBreakdownPaise'

    QuoteSnapshotResponse:
      type: object
      properties:
        quote_id:
          type: string
        estimated_distance_km:
          type: number
        estimated_duration_min:
          type: number
        fare_estimate_paise:
          type: integer
          format: int64
        surge_multiplier:
          type: number
          description: Legacy, 1 + surge_bps / 10000
        surge_bps:
          type: integer
          format: int64
        surge_reason:
          type: string
          enum: [none, peak_hours, high_demand]
        window_name:
          type: string
        discount_paise:
          type: integer
          format: int64
        coupon_code:
          type: string
        outstanding_paise:
          type: integer
          format: int64
        tax_note:
          type: string
        vehicle_type:
          type: string
        eta_to_pickup_seconds:
          type: integer
        base_fare_inr:
          type: number
          description: Legacy INR views, derived from the paise
        per_km_inr:
          type: number
        per_minute_inr:
          type: number
        minimum_fare_inr:
          type: number
        fare_estimate_inr:
          type: number
        options:
          type: array
          items:
            $ref: '#/components/schemas/QuoteOption'
        expires_at:
          type: string
          format: date-time

    CouponError:
      type: object
      description: >-
        Standard error envelope with a coupon code: COUPON_INVALID,
        COUPON_EXPIRED, COUPON_MIN_FARE, COUPON_FIRST_RIDE_ONLY,
        COUPON_USER_LIMIT_REACHED, COUPON_EXHAUSTED, COUPON_WRONG_CITY,
        COUPON_WRONG_VEHICLE, COUPONS_DISABLED.
      properties:
        error:
          type: object
          properties:
            code:
              type: string
            message:
              type: string
        meta:
          type: object
          properties:
            request_id:
              type: string

    CouponCreate:
      type: object
      required: [code, discount_type]
      properties:
        code:
          type: string
          description: 3-32 chars, letters / digits / _ / -, stored upper-case, unique case-insensitively
        description:
          type: string
        discount_type:
          type: string
          enum: [flat, percent]
        discount_value_paise:
          type: integer
          format: int64
        percent_bps:
          type: integer
        max_discount_paise:
          type: integer
          format: int64
        min_fare_paise:
          type: integer
          format: int64
        city_id:
          type: string
          format: uuid
        vehicle_types:
          type: array
          items:
            type: string
        first_ride_only:
          type: boolean
        per_user_limit:
          type: integer
        total_limit:
          type: integer
        starts_at:
          type: string
          format: date-time
        ends_at:
          type: string
          format: date-time
        is_active:
          type: boolean

    Coupon:
      allOf:
        - $ref: '#/components/schemas/CouponCreate'
        - type: object
          properties:
            id:
              type: string
              format: uuid
            used_count:
              type: integer
            created_by:
              type: string
              format: uuid
            created_at:
              type: string
              format: date-time
            updated_at:
              type: string
              format: date-time

    CustomerOutstanding:
      type: object
      properties:
        id:
          type: string
          format: uuid
        customer_user_id:
          type: string
          format: uuid
        ride_id:
          type: string
          format: uuid
          description: The cancelled ride that produced the fee
        amount_paise:
          type: integer
          format: int64
        reason:
          type: string
        status:
          type: string
          enum: [pending, settled, waived]
        settled_by_ride_id:
          type: string
          format: uuid
          description: The ride that charged (or reserved) it
        waived_by:
          type: string
          format: uuid
        waive_reason:
          type: string
        created_at:
          type: string
          format: date-time
        settled_at:
          type: string
          format: date-time

    CouponValidation:
      type: object
      properties:
        valid:
          type: boolean
        code:
          type: string
        description:
          type: string
        discount_type:
          type: string
          enum: [flat, percent]
        discount_value_paise:
          type: integer
          format: int64
        percent_bps:
          type: integer
        max_discount_paise:
          type: integer
          format: int64
        min_fare_paise:
          type: integer
          format: int64
        vehicle_types:
          type: array
          items:
            type: string
        first_ride_only:
          type: boolean

    CreateRideRequest:
      type: object
      required: [pickup, drop, vehicle_type]
      properties:
        quote_id:
          type: string
          format: uuid
        pickup:
          $ref: '#/components/schemas/Location'
        drop:
          $ref: '#/components/schemas/Location'
        vehicle_type:
          type: string
        city_id:
          type: string
          format: uuid
        payment_method:
          type: string
          enum: [cash, upi, card]
          default: cash
          description: wallet is not a ride payment method (partner subscriptions only)
        idempotency_key:
          type: string

    Location:
      type: object
      required: [address, lat, lng]
      properties:
        address:
          type: string
        lat:
          type: number
        lng:
          type: number

    Ride:
      type: object
      properties:
        id:
          type: string
          format: uuid
        customer_user_id:
          type: string
          format: uuid
        partner_id:
          type: string
          format: uuid
        vehicle_id:
          type: string
          format: uuid
        city_id:
          type: string
          format: uuid
        quote_id:
          type: string
          format: uuid
        revision:
          type: integer
        vehicle_type:
          type: string
        status:
          type: string
        pickup_address:
          type: string
        pickup_lat:
          type: number
        pickup_lng:
          type: number
        drop_address:
          type: string
        drop_lat:
          type: number
        drop_lng:
          type: number
        estimated_fare:
          type: number
        final_fare_paise:
          type: integer
          format: int64
        fare_breakdown:
          $ref: '#/components/schemas/QuoteBreakdownPaise'
        cancellation_fee_paise:
          type: integer
          format: int64
          description: Owed by the customer when they cancel (or no-show) after assignment past the rule's free window
        payment_method:
          type: string
          enum: [cash, upi, card]
        otp:
          type: string
          description: Plaintext OTP shown only to rider once the captain has arrived (sealed at rest)
        requested_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time

    RideReceipt:
      type: object
      properties:
        ride_id:
          type: string
          format: uuid
        customer_user_id:
          type: string
          format: uuid
        partner_id:
          type: string
          format: uuid
        vehicle_type:
          type: string
        status:
          type: string
        pickup_address:
          type: string
        drop_address:
          type: string
        distance_meters:
          type: integer
          description: Priced distance (quote, or tracked route when re-priced)
        duration_seconds:
          type: integer
        total_paise:
          type: integer
          format: int64
        surge_bps:
          type: integer
          format: int64
        surge_reason:
          type: string
          enum: [none, peak_hours, high_demand]
        discount_paise:
          type: integer
          format: int64
        coupon_code:
          type: string
        waiting_charge_paise:
          type: integer
          format: int64
        outstanding_paise:
          type: integer
          format: int64
        cancellation_fee_paise:
          type: integer
          format: int64
        tax_paise:
          type: integer
          format: int64
        tax_note:
          type: string
        payment_method:
          type: string
        payment_status:
          type: string
        fare_breakdown:
          $ref: '#/components/schemas/QuoteBreakdownPaise'
        reported_distance_km:
          type: number
          description: Captain telemetry, never prices the ride
        reported_duration_min:
          type: number
        tracked_distance_m:
          type: integer
          description: Server-tracked route length (haversine hops, teleports ignored)
        completed_at:
          type: string
          format: date-time
        created_at:
          type: string
          format: date-time

    RideOffer:
      type: object
      properties:
        id:
          type: string
          format: uuid
        ride_id:
          type: string
          format: uuid
        score:
          type: number
        distance_km:
          type: number
        expires_at:
          type: string
          format: date-time

    AcceptOfferResponse:
      type: object
      properties:
        ride_id:
          type: string
          format: uuid
        partner_id:
          type: string
          format: uuid
        status:
          type: string
        otp_expires_at:
          type: string
          format: date-time

    RidePayment:
      type: object
      properties:
        id:
          type: string
          format: uuid
        ride_id:
          type: string
          format: uuid
        amount_paise:
          type: integer
          format: int64
        payment_method:
          type: string
        status:
          type: string

    SharedRideView:
      type: object
      properties:
        ride_id:
          type: string
        status:
          type: string
        vehicle_type:
          type: string
        drop_address:
          type: string
        driver_first_name:
          type: string
        vehicle_registration:
          type: string
        eta_minutes:
          type: integer
