# Reviewer — the content-moderation console (queue, dashboard, needs-changes
# feedback) + its repository. Renders posts via social_domain + shared_ui.
name: feature_reviewer
description: atPost content-reviewer (moderation) feature.
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.10.7
resolution: workspace
dependencies:
  flutter:
    sdk: flutter
  atpost_core: ^0.1.0
  atpost_design: ^0.1.0
  atpost_network: ^0.1.0
  social_domain: ^0.1.0
  shared_ui: ^0.1.0
  flutter_riverpod: ^2.6.1
  go_router: ^16.3.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
