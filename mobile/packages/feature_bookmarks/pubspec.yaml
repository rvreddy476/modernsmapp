# Bookmarks — the saved-posts grid. Reads bookmarked posts from the
# social_domain kernel; no host couplings.
name: feature_bookmarks
description: atPost saved-posts (bookmarks) feature.
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.10.7
resolution: workspace
dependencies:
  flutter:
    sdk: flutter
  atpost_core: ^0.1.0
  atpost_design: ^0.1.0
  social_domain: ^0.1.0
  cached_network_image: ^3.4.1
  flutter_riverpod: ^2.6.1
  go_router: ^16.3.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
