# Live streaming — legacy v1 broadcast + LiveKit-backed v2 (list, go-live,
# viewer, broadcaster, chat panel, WHIP publisher) with its stream models,
# repositories, and provider. Session via the AuthSession contract.
name: feature_live
description: atPost live-streaming feature.
version: 0.1.0
publish_to: none
environment: {sdk: ^3.10.7}
resolution: workspace
dependencies:
  flutter: {sdk: flutter}
  atpost_core: ^0.1.0
  atpost_design: ^0.1.0
  atpost_network: ^0.1.0
  atpost_realtime: ^0.1.0
  shared_ui: ^0.1.0
  dio: ^5.9.0
  flutter_riverpod: ^2.6.1
  flutter_webrtc: ^1.4.0
  go_router: ^16.3.0
  image_picker: ^1.1.2
  livekit_client: ^2.7.2
  video_player: ^2.10.2
dev_dependencies:
  flutter_test: {sdk: flutter}
  flutter_lints: ^6.0.0
