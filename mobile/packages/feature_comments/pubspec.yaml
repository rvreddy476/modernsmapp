# Comments — the post comment thread (list, compose, delete-own). Comment
# data from the social_domain kernel; current-user id via feature_contracts.
name: feature_comments
description: atPost post-comments feature.
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.10.7
resolution: workspace
dependencies:
  flutter:
    sdk: flutter
  atpost_design: ^0.1.0
  feature_contracts: ^0.1.0
  social_domain: ^0.1.0
  flutter_riverpod: ^2.6.1
  go_router: ^16.3.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
