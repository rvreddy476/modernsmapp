<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Mopedu Captain (2026-09-18). Only what the captain uses:
        INTERNET / ACCESS_NETWORK_STATE     - the API and offer polling
        POST_NOTIFICATIONS                  - the on-duty notification and offers
                                              (runtime, 33+, asked from Home)
        ACCESS_FINE / COARSE_LOCATION       - on-duty pings, asked only after the
                                              prominent in-app disclosure
        FOREGROUND_SERVICE(_LOCATION)       - CaptainLocationService while online
      NO background location: the foreground service keeps while-in-use access
      for as long as the captain is on duty. Removed explicitly below so no
      library can merge it in.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission
        android:name="android.permission.ACCESS_BACKGROUND_LOCATION"
        tools:node="remove" />

    <application
        android:name=".CaptainApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MopeduCaptain">

        <!-- Google Maps SDK key for THIS app. No map library ships yet. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".CaptainActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden|keyboard|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Momentum's FCM service arrives with :core:notifications. It presents
          Momentum's push types, none of which a captain receives, and FCM is off
          (no google-services.json). Removed rather than left dormant.
        -->
        <service
            android:name="com.us.android.core.notifications.UsMessagingService"
            tools:node="remove" />
    </application>

</manifest>
