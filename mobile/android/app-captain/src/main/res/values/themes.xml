<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Navy window behind every frame; Compose paints the rest (UsTheme). -->
    <style name="Theme.MopeduCaptain" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/captain_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
