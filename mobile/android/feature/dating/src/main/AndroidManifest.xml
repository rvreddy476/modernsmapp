<?xml version="1.0" encoding="utf-8"?>
<!--
    Dating asks for each of these only after an in-app explanation:
    location for the profile's approximate point, panic and live sharing;
    the camera for the blink-twice selfie video (no audio is recorded).
    Declared here so only an app that ships Dating (Momentum) carries them.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />
</manifest>
