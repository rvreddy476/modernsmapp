<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Mopedu Captain (2026-09-18). Declared in the feature library, like Feast's,
      so the app that ships it carries exactly what going on duty needs:
        ACCESS_FINE / COARSE_LOCATION       - on-duty pings, asked only after the
                                              prominent in-app disclosure
        FOREGROUND_SERVICE(_LOCATION)       - CaptainLocationService while online
        POST_NOTIFICATIONS                  - the on-duty notification and offers
      NO background location: the foreground service keeps while-in-use access
      for as long as the captain is on duty. Removed explicitly so no library
      can merge it in.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.ACCESS_BACKGROUND_LOCATION"
        tools:node="remove" />

    <application>
        <!--
          On duty: a location foreground service, started only after the
          disclosure, the permission grant and the server accepting the captain online.
        -->
        <service
            android:name="com.us.android.feature.mopedu.captain.location.CaptainLocationService"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>

</manifest>
