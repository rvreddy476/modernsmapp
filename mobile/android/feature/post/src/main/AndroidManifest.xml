<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The in-app gallery grid (create hub) reads the device media library.
         API 33+ splits the permission by kind; 34+ adds the partial-access
         grant; below 33 it is the legacy storage read. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- The Audio create surface records a voice note with MediaRecorder.
         Runtime-requested with a rationale on the first tap of Record, never
         on open: the surface also offers picking an existing file, which
         needs no microphone at all. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
</manifest>
