<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Feast Kitchen (A3, 2026-09-13). Only what the kitchen uses:
        INTERNET / ACCESS_NETWORK_STATE  - the API and the order stream
        POST_NOTIFICATIONS               - the looping new-order alert (runtime, 33+,
                                           asked from the Orders tab's card)
        ACCESS_FINE / COARSE_LOCATION    - "Use my current location" on the location
                                           step, asked only after its rationale
      No camera, microphone, calls or background location.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".KitchenApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FeastKitchen">

        <!--
          Google Maps SDK key for THIS app, from
          .secrets/google-maps-android-app-kitchen.key via the application
          convention plugin. No map library ships yet; see MapPreview.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".KitchenActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|keyboardHidden|keyboard|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Momentum's FCM service arrives with :core:notifications. It presents
          Momentum's push types (chat, calls, uploads), none of which the
          kitchen receives, and FCM is disabled here (no google-services.json).
          Removed rather than left dormant; when Kitchen gets FCM it gets its
          own service for food order pushes.
        -->
        <service
            android:name="com.us.android.core.notifications.UsMessagingService"
            tools:node="remove" />
    </application>

</manifest>
