# GENERATED by scripts/gen-azure-values.sh from values-prod.yaml — DO NOT hand-edit.
# Azure (prod) overlay of the web zone 'live': ACR image, nginx ingress,
# azure-key-vault ClusterSecretStore. Re-run the script after editing the AWS values.
service:
  name: web-live
  port: 3000
image:
  repository: atpostprod454350.azurecr.io/atpost/web-live
  tag: latest
replicaCount: 3
hpa:
  enabled: true
  minReplicas: 3
  maxReplicas: 12
  targetCPUUtilizationPercentage: 65
resources:
  requests:
    cpu: 100m
    memory: 192Mi
  limits:
    cpu: 1
    memory: 768Mi
env:
  HOSTNAME: 0.0.0.0
  PORT: '3000'
  NODE_ENV: production
  API_GATEWAY_URL: http://api-gateway.atpost.svc.cluster.local:8080
  AUTH_SERVICE_URL: http://identity-auth-service.atpost.svc.cluster.local:8081
  NEXT_PUBLIC_API_BASE_URL: /live
ingress:
  enabled: true
  className: nginx
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
    nginx.ingress.kubernetes.io/proxy-body-size: 50m
  hosts:
  - host: app.cleestudio.com
    paths:
    - path: /live
      pathType: Prefix
pdb:
  enabled: true
  minAvailable: 1
