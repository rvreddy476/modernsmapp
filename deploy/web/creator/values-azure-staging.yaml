# GENERATED by scripts/gen-azure-values.sh from values-staging.yaml — DO NOT hand-edit.
# Azure (staging) overlay of the web zone 'creator': ACR image, nginx ingress,
# azure-key-vault ClusterSecretStore. Re-run the script after editing the AWS values.
service:
  name: web-creator
  port: 3000
image:
  repository: atpoststaging.azurecr.io/atpost/web-creator
  tag: latest
replicaCount: 2
hpa:
  enabled: false
  minReplicas: 2
  maxReplicas: 4
  targetCPUUtilizationPercentage: 65
resources:
  requests:
    cpu: 100m
    memory: 192Mi
  limits:
    cpu: 1
    memory: 768Mi
env:
  HOSTNAME: 0.0.0.0
  PORT: '3000'
  NODE_ENV: production
  API_GATEWAY_URL: http://api-gateway.atpost.svc.cluster.local:8080
  AUTH_SERVICE_URL: http://identity-auth-service.atpost.svc.cluster.local:8081
  NEXT_PUBLIC_API_BASE_URL: /creator
ingress:
  enabled: true
  className: nginx
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
    nginx.ingress.kubernetes.io/proxy-body-size: 50m
  hosts:
  - host: app.staging.cleestudio.com
    paths:
    - path: /creator
      pathType: Prefix
pdb:
  enabled: false
  minAvailable: 1
