# GENERATED by scripts/gen-azure-values.sh from values-staging.yaml — DO NOT hand-edit.
# Azure (staging) overlay of the service 'ai-service': ACR image, nginx ingress,
# azure-key-vault ClusterSecretStore. Re-run the script after editing the AWS values.
service:
  name: ai-service
  port: 8098
image:
  repository: atpoststaging454350.azurecr.io/atpost/ai-service
  tag: latest
replicaCount: 2
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
externalSecret:
  enabled: true
  remoteKey: atpost-staging-ai-service
  data:
  - secretKey: INTERNAL_SERVICE_KEY
    remoteRef: internal_service_key
  - secretKey: JWT_SECRET
    remoteRef: jwt_secret
  - secretKey: JWT_KID
    remoteRef: jwt_kid
  secretStoreName: azure-key-vault
env:
  ENV: staging
  LOG_LEVEL: info
  OTEL_EXPORTER_OTLP_ENDPOINT: http://tempo.observability.svc.cluster.local:4318
ingress:
  enabled: false
