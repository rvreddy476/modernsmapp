# GENERATED by scripts/gen-azure-values.sh from values-prod.yaml — DO NOT hand-edit.
# Azure (prod) overlay of the service 'monetization-service': ACR image, nginx ingress,
# azure-key-vault ClusterSecretStore. Re-run the script after editing the AWS values.
service:
  name: monetization-service
  port: 8099
image:
  repository: atpostprod454350.azurecr.io/atpost/monetization-service
  tag: latest
replicaCount: 3
hpa:
  enabled: true
  minReplicas: 3
  maxReplicas: 12
  targetCPUUtilizationPercentage: 65
resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    cpu: 1500m
    memory: 1Gi
externalSecret:
  enabled: true
  remoteKey: atpost-prod-monetization-service
  data:
  - secretKey: INTERNAL_SERVICE_KEY
    remoteRef: internal_service_key
  - secretKey: JWT_SECRET
    remoteRef: jwt_secret
  - secretKey: JWT_KID
    remoteRef: jwt_kid
  - secretKey: JWT_SECRET_PREVIOUS
    remoteRef: jwt_secret_previous
  - secretKey: JWT_KID_PREVIOUS
    remoteRef: jwt_kid_previous
  # Admin console: monetization verifies admin-service's service tokens. NOT
  # enabled yet (Monetization console routes answer 401; with writes off they
  # answer MONETIZATION_NOT_LAUNCHED once enabled). Enable in this order:
  #   1. store admin-service's key in the key vault (admin_service_token_key /
  #      admin_service_token_kid) and its public half and kid here
  #      (caller_admin_pubkey / caller_admin_kid);
  #   2. uncomment below AND the SERVICE_CALLERS lines under env;
  #   3. enable admin-service's two lines. SERVICE_CALLERS before the key refuses boot.
  # - secretKey: SERVICE_CALLER_ADMIN_SERVICE_PUBKEY
  #   remoteRef: caller_admin_pubkey
  # - secretKey: SERVICE_CALLER_ADMIN_SERVICE_KID
  #   remoteRef: caller_admin_kid
  secretStoreName: azure-key-vault
env:
  ENV: prod
  # Admin console (see externalSecret above for the order): uncomment
  # together with SERVICE_CALLER_ADMIN_SERVICE_PUBKEY / _KID.
  # SERVICE_CALLERS: 'admin-service'
  # SERVICE_CALLER_ADMIN_SERVICE_OPS: 'monetization:stats.read,monetization:fraud.review,monetization:wallet.freeze,monetization:wallet.unfreeze,monetization:wallet.rebuild,monetization:fund.read,monetization:fund.rates,monetization:creators.suspend,monetization:fund.settle,monetization:fund.reverse,monetization:fund.budget,monetization:disputes.read,monetization:disputes.act,monetization:refund.issue,monetization:payouts.read,monetization:audit.read'
  MONETIZATION_WRITES_ENABLED: "false"
  MONETIZATION_PAYOUTS_ENABLED: "false"
  MONETIZATION_MAINTENANCE: "false"
  MONETIZATION_TDS_APPLY: "false"
  LOG_LEVEL: info
  OTEL_EXPORTER_OTLP_ENDPOINT: http://tempo.observability.svc.cluster.local:4318
ingress:
  enabled: false
pdb:
  enabled: true
  minAvailable: 2
