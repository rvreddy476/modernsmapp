# GENERATED by scripts/gen-azure-values.sh from values-staging.yaml — DO NOT hand-edit.
# Azure (staging) overlay of the service 'media-service': ACR image, nginx ingress,
# azure-key-vault ClusterSecretStore. Re-run the script after editing the AWS values.
service:
  name: media-service
  port: 8087
image:
  repository: atpoststaging.azurecr.io/atpost/media-service
  tag: latest
replicaCount: 2
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
externalSecret:
  enabled: true
  remoteKey: atpost-staging-media-service
  data:
  - secretKey: INTERNAL_SERVICE_KEY
    remoteRef: internal_service_key
  - secretKey: JWT_SECRET
    remoteRef: jwt_secret
  - secretKey: JWT_KID
    remoteRef: jwt_kid
  - secretKey: JWT_PUBLIC_KEY_PEM
    remoteRef: jwt_public_key_pem
  secretStoreName: azure-key-vault
env:
  ENV: staging
  LOG_LEVEL: info
  OTEL_EXPORTER_OTLP_ENDPOINT: http://tempo.observability.svc.cluster.local:4318
  JWT_RS256_KID: rsa-1
ingress:
  enabled: false
