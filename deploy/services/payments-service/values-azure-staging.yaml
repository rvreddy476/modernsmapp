# GENERATED by scripts/gen-azure-values.sh from values-staging.yaml — DO NOT hand-edit.
# Azure (staging) overlay of the service 'payments-service': ACR image, nginx ingress,
# azure-key-vault ClusterSecretStore. Re-run the script after editing the AWS values.
service:
  name: payments-service
  port: 8102
image:
  repository: atpoststaging454350.azurecr.io/atpost/payments-service
  tag: "3bf3601cca56c71a9eb4c556a964f316260115eb"
replicaCount: 2
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
externalSecret:
  enabled: true
  remoteKey: atpost-staging-payments-service
  data:
  - secretKey: INTERNAL_SERVICE_KEY
    remoteRef: internal_service_key
  - secretKey: JWT_SECRET
    remoteRef: jwt_secret
  - secretKey: JWT_KID
    remoteRef: jwt_kid
  # admin-service (admin console) as a payments caller is NOT enabled on this
  # overlay; SERVICE_CALLERS is not configured here yet. When it is, follow
  # values-prod.yaml's numbered steps: key-vault caller_admin_pubkey /
  # caller_admin_kid, then these lines and SERVICE_CALLER_ADMIN_SERVICE_OPS,
  # then add admin-service to SERVICE_CALLERS last (earlier refuses boot).
  # - secretKey: SERVICE_CALLER_ADMIN_SERVICE_PUBKEY
  #   remoteRef: caller_admin_pubkey
  # - secretKey: SERVICE_CALLER_ADMIN_SERVICE_KID
  #   remoteRef: caller_admin_kid
  secretStoreName: azure-key-vault
env:
  ENV: staging
  LOG_LEVEL: info
  OTEL_EXPORTER_OTLP_ENDPOINT: http://tempo.observability.svc.cluster.local:4318
ingress:
  enabled: false
