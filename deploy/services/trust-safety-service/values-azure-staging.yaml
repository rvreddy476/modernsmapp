# GENERATED by scripts/gen-azure-values.sh from values-staging.yaml — DO NOT hand-edit.
# Azure (staging) overlay of the service 'trust-safety-service': ACR image, nginx ingress,
# azure-key-vault ClusterSecretStore. Re-run the script after editing the AWS values.
service:
  name: trust-safety-service
  port: 8091
image:
  repository: atpoststaging454350.azurecr.io/atpost/trust-safety-service
  tag: "3bf3601cca56c71a9eb4c556a964f316260115eb"
replicaCount: 2
resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi
externalSecret:
  enabled: true
  remoteKey: atpost-staging-trust-safety-service
  data:
  - secretKey: INTERNAL_SERVICE_KEY
    remoteRef: internal_service_key
  - secretKey: JWT_SECRET
    remoteRef: jwt_secret
  - secretKey: JWT_KID
    remoteRef: jwt_kid
  # Admin console: trust-safety verifies admin-service's service tokens on
  # /v1/internal/admin/trust/*. NOT enabled yet. Enable in this order:
  #   1. store admin-service's private seed and kid in its key vault entry
  #      (admin_service_token_key / admin_service_token_kid) and the public
  #      half and kid here (caller_admin_pubkey / caller_admin_kid);
  #   2. uncomment below AND the SERVICE_CALLERS lines under env;
  #   3. enable admin-service's lines.
  # Setting SERVICE_CALLERS before the key exists refuses boot.
  # - secretKey: SERVICE_CALLER_ADMIN_SERVICE_PUBKEY
  #   remoteRef: caller_admin_pubkey
  # - secretKey: SERVICE_CALLER_ADMIN_SERVICE_KID
  #   remoteRef: caller_admin_kid
  secretStoreName: azure-key-vault
env:
  ENV: staging
  LOG_LEVEL: info
  # Admin console: uncomment with SERVICE_CALLER_ADMIN_SERVICE_PUBKEY / _KID.
  # SERVICE_CALLERS: admin-service
  # SERVICE_CALLER_ADMIN_SERVICE_OPS: trust_safety:stats.read,trust_safety:reports.read,trust_safety:reports.act,trust_safety:appeals.read,trust_safety:appeals.act,trust_safety:grievances.read,trust_safety:grievances.act,trust_safety:strikes.read,trust_safety:strikes.manage,trust_safety:verification.review,trust_safety:media_labels.read,trust_safety:keyword_filters.read,trust_safety:audit.read
  OTEL_EXPORTER_OTLP_ENDPOINT: http://tempo.observability.svc.cluster.local:4318
ingress:
  enabled: false
