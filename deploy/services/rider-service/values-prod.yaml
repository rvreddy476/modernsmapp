# rider-service overrides for the prod environment.

service:
  name: rider-service
  port: 8116

image:
  repository: '{{ required "global.awsAccountId is required" .Values.global.awsAccountId }}.dkr.ecr.ap-south-1.amazonaws.com/atpost/rider-service'
  tag: latest # CI overrides via --set image.tag=<sha>

replicaCount: 3
hpa:
  enabled: true
  minReplicas: 3
  maxReplicas: 12
  targetCPUUtilizationPercentage: 65

resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    cpu: 1500m
    memory: 1Gi

serviceAccount:
  irsaRoleArn: 'arn:aws:iam::{{ required "global.awsAccountId is required" .Values.global.awsAccountId }}:role/atpost-prod-rider-service-irsa'

externalSecret:
  enabled: true
  remoteKey: atpost/prod/rider-service
  data:
    - { secretKey: INTERNAL_SERVICE_KEY, remoteRef: internal_service_key }
    - { secretKey: JWT_SECRET, remoteRef: jwt_secret }
    - { secretKey: JWT_KID, remoteRef: jwt_kid }
    - { secretKey: JWT_SECRET_PREVIOUS, remoteRef: jwt_secret_previous }
    - { secretKey: JWT_KID_PREVIOUS, remoteRef: jwt_kid_previous }
    # Admin console (Mopedu): rider-service verifies admin-service's service
    # tokens on /v1/rider/internal/admin/*. NOT enabled yet: without it the
    # console's Mopedu routes are refused (401). To enable, in this order:
    #   1. the founder generates admin-service's ed25519 key (one key for
    #      every product): the private half (base64 32-byte seed) and kid go
    #      to admin-service's secret store (admin_service_token_key /
    #      admin_service_token_kid); the public half and the same kid go here
    #      as caller_admin_pubkey / caller_admin_kid;
    #   2. uncomment the two lines below AND the SERVICE_CALLERS lines under env;
    #   3. enable admin-service's two lines (its values file).
    # Setting SERVICE_CALLERS before the key exists refuses boot.
    # - { secretKey: SERVICE_CALLER_ADMIN_SERVICE_PUBKEY, remoteRef: caller_admin_pubkey }
    # - { secretKey: SERVICE_CALLER_ADMIN_SERVICE_KID, remoteRef: caller_admin_kid }
    # Pricing engine secrets. BOTH are required before the next rider-service
    # deploy: with ENV=prod the service refuses to start without them.
    #   RIDER_PII_KEYS: ride OTPs sealed at rest (scope rider.ride_otp),
    #     "v1:<base64 32-byte key>", the FOOD_PII_KEYS format; generate with
    #     `openssl rand -base64 32`. Never a dev key.
    #   MOPEDU_PLATFORM_GSTIN: the platform's real e-commerce-operator GSTIN,
    #     liable for ride-fare GST under s.9(5).
    #   GOOGLE_MAPS_SERVER_KEY: optional; without it quotes use the haversine
    #     estimate. Restrict the key to the Routes API and this cluster's egress.
    # - { secretKey: RIDER_PII_KEYS, remoteRef: rider_pii_keys }
    # - { secretKey: MOPEDU_PLATFORM_GSTIN, remoteRef: mopedu_platform_gstin }
    # - { secretKey: GOOGLE_MAPS_SERVER_KEY, remoteRef: google_maps_server_key }
    # Payments lane: rider-service's payments-service token (upi / card ride
    # fares, application mopedu, reference type mopedu_ride) is NOT enabled
    # yet. Without it the intent routes answer 503 PAYMENTS_UNAVAILABLE and
    # cash rides are unaffected; boot is not refused. Enable payments-service
    # first (caller_rider_pubkey / caller_rider_kid, rider-service in its
    # SERVICE_CALLERS), then uncomment the two lines below. Never reuse a
    # staging key here.
    # - { secretKey: RIDER_SERVICE_TOKEN_KEY, remoteRef: rider_service_token_key }
    # - { secretKey: RIDER_SERVICE_TOKEN_KID, remoteRef: rider_service_token_kid }

env:
  ENV: prod
  LOG_LEVEL: info
  OTEL_EXPORTER_OTLP_ENDPOINT: http://tempo.observability.svc.cluster.local:4318
  # Coupons stay off in production until the tax adviser confirms the GST
  # treatment of a discount (the ride fare is taxed net of the coupon under
  # s.9(5); the same open question as FOOD_COUPONS_ENABLED). Estimates with
  # a coupon_code answer 422 COUPONS_DISABLED while this is false.
  MOPEDU_COUPONS_ENABLED: "false"
  # Demand surge ceiling in basis points (5000 = x1.5); peak / night fare
  # windows are database rows and are not capped by this.
  MOPEDU_SURGE_CAP_BPS: "5000"
  # Payments lane (see externalSecret above): unused until
  # RIDER_SERVICE_TOKEN_KEY / _KID are set.
  PAYMENTS_SERVICE_URL: http://payments-service.atpost.svc.cluster.local:8102
  # Launch safety: selfie face compare through media-service (see the
  # staging values for the rule). MEDIA_SERVICE_URL is REQUIRED in
  # production — rider-service refuses to start without it; "mock" is
  # refused here.
  MOPEDU_FACE_COMPARE_MODE: http
  MEDIA_SERVICE_URL: http://media-service.atpost.svc.cluster.local:8087
  MOPEDU_SELFIE_MIN_SIMILARITY: "80"
  # Admin console (see externalSecret above for the order): uncomment
  # together with SERVICE_CALLER_ADMIN_SERVICE_PUBKEY / _KID. OPS is rider's
  # AdminPermissions (rider-service/internal/http/admin_token.go).
  # SERVICE_CALLERS: 'admin-service'
  # SERVICE_CALLER_ADMIN_SERVICE_OPS: 'rider:stats.read,rider:partners.read,rider:partners.approve,rider:partners.suspend,rider:documents.review,rider:vehicles.review,rider:payments.read,rider:payments.settle,rider:payments.reject,rider:rides.read,rider:rides.cancel,rider:ratings.moderate,rider:complaints.act,rider:incidents.read,rider:incidents.act,rider:incidents.reveal,rider:cities.manage,rider:fares.manage,rider:reports.read,rider:audit.read'

ingress:
  enabled: false

pdb:
  enabled: true
  minAvailable: 2
