-- reviewer-service base schema lives in setup.sql (applied first). This file
-- exists so the embedded migrations FS is non-empty and to reserve the
-- migration sequence for future additive changes.
SELECT 1;
